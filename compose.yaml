services: 

  postgres:
    image: postgis/postgis:16-master
    container_name: PostgresDB
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=pet-radar
    volumes:
      - postgresdb_data:/var/lib/postgresql

volumes: 
  postgresdb_data: